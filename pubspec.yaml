name: metricestan
version: 1.0.0
environment:
  sdk: ">=3.3.0 <4.0.0"
publish_to: 'none'

dependencies:
  dotenv: ^4.2.0
  http: ^1.6.0
  mongo_dart: ^0.10.3
  redis: ^4.0.0

dev_dependencies:
  lints: ^6.0.0
  protoc_plugin: ^25.0.0
